<br>
<%= link_to @user.email.split('@').first, user_path(@user.id) %>
<%if user_signed_in? && current_user.id == tweet.user_id%>
  <%=link_to "Edit", edit_tweet_path(tweet.id), class:"btn btn-secondary"%><br>
  <%=link_to "Delete", tweet_path(tweet.id), method: :delete, class:"btn btn-secondary"%><br>

<%end%>

<%= tweet.body%><br>
