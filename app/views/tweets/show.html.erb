<br>
<%if user_signed_in? && current_user.id == @tweet.user_id%>
  <%=link_to "Редагувати", edit_tweet_path(@tweet.id)%><br>
  <%=link_to "Destroy", tweet_path(@tweet.id), method: :delete%><br>
<%end%>

YOUR TWEET:<%= @tweet.body%><br>
<%= render 'comments/create'%>
<% @tweet.user.comments.each do |x| %>
<%= x.text %>
<% end %>
