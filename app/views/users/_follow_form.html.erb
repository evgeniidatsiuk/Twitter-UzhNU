<div class="row">
  <div class="col center">
    <% unless current_user == @user %>
      <% if current_user.following?(@user) %>
        <%= render 'users/unfollow' %>
      <% else %>
        <%= render 'users/follow' %>
      <% end %>
    <% end %>
  </div>
</div>
